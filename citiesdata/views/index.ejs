<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="http://fonts.googleapis.com/css?family=Roboto:300,400,500,700" type="text/css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="https://code.getmdl.io/1.3.0/material.indigo-pink.min.css">
    <style>
        h1 {
            font-size: 20pt;
            margin: 0;
        }

        h2 {
            font-size: 16pt;
            margin: 0;
        }

        .mdl-list__item {
            height: 30px;
        }
        .link {
            text-decoration: none;
            
        }

        /*Hakutoiminnon tyylittely*/
        .mdl-grid {
            font-size: 10px;
        }

        .mdl-cell {
            font-size: 10px;
        }
        .mdl-textfield {
            width: 100%;
        }
        .mdl-textfield__input {
           
            box-sizing: border-box;
            width: 100%;
            padding-top: 1rem;
            border-radius: 4px;
            border: 1px solid #3f51b5;
            color: black;

        }
        .mdl-textfield__input:not(:placeholder-shown) + .mdl-textfield__label {
            display: none;
            
         
        }
        .mdl-textfield__label {
            display: block;
            width: 100%;
            padding-top: 0rem;
            padding-bottom: 0rem;
            margin-top: 5%;
            color: black;
        }
        
        .mdl-button {
            margin-top: 15%;
            box-sizing: border-box;
            width: 90%;
        }
        /* Taulukon tyylittely */
        .mdl-layout {
            margin-top: 15px;
            margin-left: auto;
            margin-right: auto;
            width: 40%;
        }

        .material-icons {
            color: gray;
        }
        .sort-icon {
            display: inline-block;
            width: 24px;
            text-align: center;
        }
        /* Alimman rivin kohdennusta vajaan solumäärän vuoksi*/
        .mdl-data-table tr::after {
            content: "";
            display: table-cell;
            border-bottom: 1px solid lightgray;

        }
    
                
    </style>
    <title>oppimistehtava-1-kuntien-asukastilastoja-bhaoj002xamk</title>
</head>
<body>
    <div class="mdl-layout">
        
        <h2  class="mdl-typography--display-3">Kuntien asukastilastoja</h2>
        <!-- Hakukenttä -->
        <form method="GET" action="/" class="mdl-grid">
            <div class="mdl-cell mdl-cell--6-col">
                <div class="mdl-textfield mdl-js-textfield">
                    <input class="mdl-textfield__input" type="text" id="search" name="search" value="<%= search %>">
                    <label class="mdl-textfield__label" for="search">Hae kunta</label>
                   
                </div>
            </div>
            <div class="mdl-cell mdl-cell--6-col">
                <button class="mdl-button mdl-js-button mdl-button--raised mdl-button--colored" type="submit">
                    Search
                </button>
            </div>
        </form>
        <!-- Table taulukko alkaa -->
        <table class="mdl-data-table mdl-js-data-table mdl-shadow--20dp">
            <thead>
                <tr>
                    <th class="mdl-data-table__cell--non-numeric">
                        <!-- Lajittelu-linkki kunnille-->
                        <a class="mdl-color-text--primary link" 
                            href="?search=<%= search %>&sort=kunta&order=<%= sort === 'kunta' && order === 'desc' ? 'asc' : 'desc' %>">
                            Kunta
                            <span class="sort-icon">
                                <% if (sort==='kunta') { %>
                                    <i class="material-icons">
                                        <%= order==='asc' ? 'arrow_upward' : 'arrow_downward' %>
                                    </i>
                                <% } else if (!sort) { %>
                                    <i class="material-icons">
                                        arrow_upward
                                    </i>
                                <% } %>
                            </span>
                        </a>
                    </th>
                    <th class="mdl-data-table__cell--non-numeric">
                        <!-- Lajittelu-linkki asukasmäärät kunnassa-->
                        <a class="mdl-color-text--green link"
                            href="?search=<%= search %>&sort=asukkaatYhteensa&order=<%= sort === 'asukkaatYhteensa' && order === 'asc' ? 'desc' : 'asc' %>">
                            Yhteensa
                            <span class="sort-icon">
                                <% if (sort==='asukkaatYhteensa' ) { %>
                                    <i class="material-icons">
                                        <%= order==='asc' ? 'arrow_upward' : 'arrow_downward' %>
                                    </i>
                                    <% } %>
                            </span>
                        </a>
                    </th>
                    <th class="mdl-data-table__cell--non-numeric">
                         <!-- Lajittelu-linkki miesten määrä kunnassa-->
                        <a class="mdl-color-text--blue link"
                            href="?search=<%= search %>&sort=asukkaatMiehet&order=<%= sort === 'asukkaatMiehet' && order === 'asc' ? 'desc' : 'asc' %>">
                            Miehet
                            <span class="sort-icon">
                                <% if (sort==='asukkaatMiehet' ) { %>
                                    <i class="material-icons">
                                        <%= order==='asc' ? 'arrow_upward' : 'arrow_downward' %>
                                    </i>
                                    <% } %>
                            </span>
                        </a>
                    </th>
                    <th class="mdl-data-table__cell--non-numeric">
                          <!-- Lajittelu-linkki naisten määrä kunnassa-->
                        <a class="mdl-color-text--red link"
                            href="?search=<%= search %>&sort=asukkaatNaiset&order=<%= sort === 'asukkaatNaiset' && order === 'asc' ? 'desc' : 'asc' %>">
                            Naiset
                            <span class="sort-icon">
                                <% if (sort==='asukkaatNaiset' ) { %>
                                    <i class="material-icons">
                                        <%= order==='asc' ? 'arrow_upward' : 'arrow_downward' %>
                                    </i>
                                    <% } %>
                            </span>
                        </a>
                    </th>

                </tr>
            </thead>
            <tbody>
                  <!-- Foreach silmukalla iteroidaan database(data.db) tiedot Prisman toimintojen mukaisesti -->
                <% kunnat.forEach((kunta)=> { %>
                    <tr>
                        <td class="mdl-data-table__cell--non-numeric">
                            <%= kunta.kunta %>
                        </td>
                        <td class="mdl-data-table__cell--non-numeric">
                            <%= kunta.asukkaatYhteensa.toLocaleString('fi-FI') %>
                        </td>
                        <td class="mdl-data-table__cell--non-numeric">
                            <%= kunta.asukkaatMiehet.toLocaleString('fi-FI') %>
                        </td>
                        <td class="mdl-data-table__cell--non-numeric">
                            <%= kunta.asukkaatNaiset.toLocaleString('fi-FI') %>
                        </td>

                    </tr>

                    <% }) %>
                        <tr>
                            <th class="mdl-data-table__cell--non-numeric">Kunnat</th>
                            <th class="mdl-data-table__cell--non-numeric">Keskiarvo</th>
                            <th></th>
                            <th class="mdl-data-table__cell--non-numeric">Naiset %</th>

                        </tr>
                        <tr>
                            <td class="mdl-data-table__cell--non-numeric">
                                <%= totalKunta.toLocaleString() %>
                            </td>
                            <td class="mdl-data-table__cell--non-numeric">
                                <%= totalAsukkaatKeskiarvo.toLocaleString('fi-FI') %>
                            </td>

                            <td></td>
                            <td class="mdl-data-table__cell--non-numeric">
                                <%= totalAsukkaatNaisetProsentti.toFixed(2)%>
                            </td>

                        </tr>
            </tbody>
        </table>




    </div>

</body>

</html>