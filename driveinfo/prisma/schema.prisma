generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./data.db"
}

model Ajopaiva {
  id       Int    @id @default(autoincrement())
  reitti   String
  matka    Int
  kayttaja Int
  pvm      String
}
